{
  "name": "server-monitor",
  "version": "1.0.0",
  "description": "Professional server monitoring tool with agents",
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && yarn dev",
    "dev:web": "cd apps/web && yarn dev",
    "dev:agent": "cd apps/agent && yarn dev",
    "build:types": "cd packages/types && yarn build",
    "build:api": "cd apps/api && yarn build",
    "build:web": "cd apps/web && yarn build",
    "build:agent": "cd apps/agent && yarn build",
    "build": "yarn build:types && yarn build:api && yarn build:web && yarn build:agent",
    "install:all": "cd packages/types && yarn install && cd ../../apps/api && yarn install && cd ../web && yarn install && cd ../agent && yarn install",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "packageManager": "yarn@1.22.22",
  "engines": {
    "node": ">=22.0.0",
    "yarn": ">=1.22.0"
  }
}